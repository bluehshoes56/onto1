  Files to transfer

  news/
  ├── generate_dashboard.py    ← Main script
  ├── requirements.txt         ← Dependencies
  ├── Prompt Claude.docx       ← Instructions
  └── URL.docx                 ← News sources

  ---
  On your Mac after transfer

  # 1. Navigate to folder
  cd ~/path/to/news

  # 2. Install dependencies
  pip3 install -r requirements.txt

  # 3. Edit script to add your Azure credentials
  nano generate_dashboard.py
  # (fill in AZURE_OPENAI_ENDPOINT, API_KEY, DEPLOYMENT)

  # 4. Run
  python3 generate_dashboard.py

  # 5. Open dashboard
  open payments_dashboard.html