# --- core (needed to run the demo with the mock LLM and networkx graph) ---
duckdb>=1.0
pandas>=2.0
numpy>=1.26
pydantic>=2.0
networkx>=3.0
pytest>=8.0

# --- production swaps (uncomment as you move off the local stand-ins) ---
# requests>=2.31        # LLM_BACKEND=ollama
# boto3>=1.34           # LLM_BACKEND=bedrock  (AWS)
# neo4j>=5.0            # GRAPH_BACKEND=neo4j  (Neo4j Community / Aura / Neptune)
# dbt-duckdb>=1.7       # run the semantic layer through dbt locally
# statsforecast>=1.7    # swap the forecaster for AutoARIMA
# prefect>=2.0          # runtime Ralph loop scheduling locally
